export enum Split {
    none = "None",
    kid = "Kid",
    adult = "Player"
}

export enum Category {
    home = "Home",
    out = "Out",
    caregiving = "Caregiving",
    magic = "Magic",
    wild = "Wild"
}

export enum Status {
    inPlay = "In Play",
    notInPlay = "Not In Play",
    inLimbo = "In Limbo"
};
